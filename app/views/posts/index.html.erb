<div class="p-posts">
  <h1 class="p-posts__title">びびらせ王選手権</h1>
  <p class="p-home__info">ここでは新着順に並びます。<br>TOPページにランキング5位までが表示されます。</p>
  <ul class="c-posts__list">
    <% @posts.each do |post| %>
    <li class="c-posts__item">
      <%= link_to post_path(post.id) do %>
        <p class="c-posts__item-body">
          <%= post.body %>
        </p>
        <p class="c-posts__item-name">
          <%= post.user_name %>
        </p>
        <% end %>
    </li>
  <% end %>
  </ul>

  <%= paginate @posts %>
</div>